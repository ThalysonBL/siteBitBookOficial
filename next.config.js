/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: ".",
    images: {
      unoptimized: true,
    },
  reactStrictMode: true,
  swcMinify: true,
  
  
  
};

module.exports = nextConfig;
